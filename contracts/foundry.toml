[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
monad_testnet = "${MONAD_TESTNET_RPC}"
monad_mainnet = "${MONAD_RPC}"

[etherscan]
monad = { key = "${MONAD_EXPLORER_KEY}", url = "https://api.monadvision.com/api" }
